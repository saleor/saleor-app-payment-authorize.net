query ProductList {
	products(
		first: 1
		channel: "ken-r"
		where: { isAvailable: true, giftCard: false, ids: ["UHJvZHVjdDoyNjA3MA=="] }
		sortBy: { field: PRICE, direction: DESC }
	) {
		edges {
			node {
				id
				name
				thumbnail(size: 2048) {
					url
					alt
				}
				category {
					name
				}
				defaultVariant {
					id
					name
					pricing {
						price {
							gross {
								amount
								currency
							}
						}
					}
				}
			}
		}
	}
}
