mutation PaymentGatewayInitialize($checkoutId: ID!, $paymentGateway: String!) {
	paymentGatewayInitialize(id: $checkoutId, paymentGateways: [{ id: $paymentGateway }]) {
		gatewayConfigs {
			id
			data
			errors {
				field
				message
				code
			}
		}
		errors {
			field
			message
			code
		}
	}
}
